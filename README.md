# Telinha

Peer to peer screen sharing for Windows, over the internet.

Telinha transmits a screen or a single application window, together with the
audio produced by that same source. It is a one way transmission tool, not a
communication tool: there is no chat, no microphone capture and no call
signalling beyond what is required to establish the peer to peer link.

## Status

Early development. See `docs/NOTES.md` for the current roadmap and
`docs/adr/` for the architectural decisions behind it.
